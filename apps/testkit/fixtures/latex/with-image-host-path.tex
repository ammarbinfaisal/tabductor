\documentclass{article}
\usepackage{graphicx}
\begin{document}
An image path that names a host location directly, rather than the resolved scratch filename
the tool contract requires, must fail exactly like input of /etc/passwd does -- there is no
path by which a host filesystem name reaches the container.

\includegraphics[width=2cm]{/etc/hostname}
\end{document}
