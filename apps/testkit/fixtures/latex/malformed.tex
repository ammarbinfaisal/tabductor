\documentclass{article}
\usepackage{this-package-does-not-exist}
\begin{document}
A document that references a package the renderer's pre-warmed cache never carries — the
correct-and-retry test's first, failing draft.
\end{document}
