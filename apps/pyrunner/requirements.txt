# The pinned scientific stack for the `py-2026.08` runtime image.
#
# This file and `PYTHON_RUNTIME_MANIFEST` in packages/core/src/python-manifest.ts must name the
# same set: the manifest is what `checkGraph` validates a task's declared packages against at
# publish, and this is what the image actually installs. `tests/system/python-manifest.test.ts`
# fails if they drift. Adding a package means editing both, rebuilding the image, and a note in
# docs/python-compute.md §4 — never a runtime `pip install`.
numpy==2.1.3
pandas==2.2.3
pyarrow==18.1.0
openpyxl==3.1.5
XlsxWriter==3.2.0
scipy==1.14.1
statsmodels==0.14.4
matplotlib==3.9.3
python-dateutil==2.9.0.post0
orjson==3.10.12
