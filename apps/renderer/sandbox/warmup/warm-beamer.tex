% Pre-warm target for the beamer/deck path (`assets.render(format: "deck")`, §18.11 — a "PDF
% deck", never a `.pptx`). Build-time only, same reasoning as warm-article.tex.
\documentclass{beamer}
\usetheme{default}
\usepackage{graphicx}
\begin{document}
\begin{frame}{Warmup deck}
A deck slide.
\end{frame}
\end{document}
